/**
 * @name comRequest
 * @description 通用请求拦截器
 */

import type { FetchContext } from 'ofetch'

export function comRequest({ request, options, error }: FetchContext) {
  // options.retry = 3
  // options.retryDelay = 300
  // options.timeout = 1000
}
